{
  "name": "cipher",
  "private": true,
  "description": "Cipher",
  "version": "1.0.0",
  "scripts": {
    "dev": "vite dev src",
    "build": "vite build src --outDir ../dist --emptyOutDir",
    "preview": "vite preview",
    "deploy": "vite build src --outDir ../dist --emptyOutDir && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^3.2.3",
    "vite": "^2.9.0"
  },
  "engines": {
    "node": ">= 14.x"
  }
}
